<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Identical to the main config except that plain HTTP is
  allowed to 10.0.2.2, which is the host machine as seen from the Android
  emulator, so instrumentation tests can reach the Firebase emulators there.
  Release builds use the main config and permit no cleartext anywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">openfoodfacts.org</domain>
        <domain includeSubdomains="true">world.openfoodfacts.org</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
